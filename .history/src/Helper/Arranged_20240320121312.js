/** @format */

import { LazyLoadImage } from "react-lazy-load-image-component";

const LazyImage = ({ src }) => {
  return (
    <LazyLoadImage
      alt={"alt"}
      src={src} 
      clas
    />
  );
};
